fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Image processing
Pillow>=10.0.0

# Tampering detector: trained Random Forest on classical forensic features
# (real, validated -- see weights/SPLICE_MODEL_CARD.md for methodology/numbers)
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0

# AI Models — backbones load real pretrained weights (verified working).
# Deepfake/experimental-ViT classifier heads are NOT fine-tuned: scores are
# deterministic per-image but carry no learned signal until trained on
# labeled data (FaceForensics++ for deepfake_model; ViT path in
# tampering_model.py is experimental/unused pending CASIA v2 fine-tuning).
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0           # EfficientNet
transformers>=4.35.0  # ViT
huggingface_hub>=0.20.0  # optional HF Hub fallback for deepfake weights, see TRUSTLENS_DEEPFAKE_HF_REPO

# Training scripts only (training/build_deepfake_manifest.py, finetune_deepfake.py)
pandas>=2.0.0
tqdm>=4.65.0
kagglehub>=0.3.0
# imagehash            # optional, stronger near-duplicate detection across dataset mirrors

# Optional integrations
# google-cloud-vision   # Real reverse search
# piexif                # Advanced EXIF manipulation
